let obj = {
    chave1: 1,
    chave2: 2,
    chave3: 3,
};

// chave1: 1 
// chave1: chave da propriedade
// 1: valor da propriedade


console.log(obj);

console.log(Object.keys(obj));

// verificando as chaves do obj
// recebendo o array
